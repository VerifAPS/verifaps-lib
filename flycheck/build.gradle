
description = 'Flycheck'
dependencies {
    compile project(':lang')
    compile project(':xml')
    compile project(':symbex')
    //compile group: 'com.xenomachina', name: 'kotlin-argparser', version: '2.0.4'
    compile group: 'io.github.microutils', name: 'kotlin-logging', version: '1.6.26'
    implementation 'com.github.ajalt:clikt:2.1.0'
    testCompile group: 'org.jetbrains.kotlin', name: 'kotlin-test-junit', version: '1.3.40'
}

task fatJar(type : Jar) {
    with jar
}

kotlin {
    experimental {
        coroutines "error"
    }
}